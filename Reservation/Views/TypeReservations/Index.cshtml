@model IEnumerable<Reservation.Data.TypeReservation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreApp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreApp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_type">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_type">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_type">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
